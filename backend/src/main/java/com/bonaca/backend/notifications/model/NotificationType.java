package com.bonaca.backend.notifications.model;

/** Matches the 3 notification `type` values src/features/notifications/NotificationsScreen.tsx's mock data uses. */
public enum NotificationType {
    METRIC_ANOMALY,
    SUBSCRIPTION,
    PAYMENT_REQUEST
}
